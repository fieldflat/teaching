<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title></title>

  <style>
    .container {
      max-width:800px;
      border: solid 1px #FF773E;
      border-radius:10px 10px 0px 0px;
    }

    .header {
      background: #FF773E;
      border-radius:10px 10px 0px 0px;
      padding: 15px;
      font-size: 25px;
    }

    fieldset{
      padding: 15px;
    }
  </style>
</head>
<body>

  <div class="container my-5">
    <div class="row header">
      <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span>
      ログイン
    </div>
    <%= form_tag("/users/login") do %>
    <form>
      <fieldset class="form-group">
        <div class="form-group">
          <legend>名前</legend>
          <input type="text" class="form-control" name="name" value="<%= @user.name %>">
        </div>
        <div class="form-group">
          <legend>email</legend>
          <input type="text" class="form-control" placeholder="example@i.softbank.jp" name="email" value="<%= @user.email %>">
        </div>
        <div class="form-group">
          <legend>パスワード</legend>
          <input class="form-control" type="password" name="password">
        </div>
        <% @user.errors.full_messages.each do |message| %>
          <li class="text-danger my-1" style="list-style: none;"><%= message %></li>
        <% end %>
        <% if @login_error_message %>
          <p class="text-danger"><%= @login_error_message %></p>
        <% end %>
        <div class="text-center">
        <button type="submit" class="btn btn-primary pull-right">ログイン</button>
        </div>
      </fieldset>
    </form>
    <% end %>
  </div>

</body>
</html>
