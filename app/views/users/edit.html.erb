<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title></title>

  <style>
    .container {
      max-width:400px;
      border: solid 1px #FF773E;
      border-radius:10px 10px 0px 0px;
    }

    .header {
      background: #FF773E;
      border-radius:10px 10px 0px 0px;
      padding: 15px;
      font-size: 25px;
    }

    fieldset{
      padding: 15px;
    }
  </style>
</head>
<body>

  <div class="container my-5">
    <div class="row header">
      <span class="glyphicon glyphicon-log-in" aria-hidden="true"></span>
      ユーザ情報の編集
    </div>
    <%= form_tag("/users/#{@user.id}/update") do %>
    <form>
      <fieldset class="form-group">
        <div class="form-group">
          <legend>名前</legend>
          <input type="text" class="form-control" name="name" value="<%= @user.name %>">
        </div>
        <div class="form-group">
          <legend>自己紹介</legend>
          <textarea name="introduction" class="form-control" rows="4" ><%= @user.introduction %></textarea>
        </div>
        <% @user.errors.full_messages.each do |message| %>
          <li class="text-danger my-1" style="list-style: none;"><%= message %></li>
        <% end %>
        <% if @login_error_message %>
          <p class="text-danger"><%= @login_error_message %></p>
        <% end %>
        <div class="text-center">
        <button type="submit" class="btn btn-primary pull-right">アップデート</button>
        </div>
      </fieldset>
    </form>
    <% end %>
  </div>

</body>
</html>
